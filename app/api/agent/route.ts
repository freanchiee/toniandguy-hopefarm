export async function POST() {
  return Response.json({ ok: true, message: "Agent endpoint ready for Phase 3." });
}

export async function GET() {
  return Response.json({ ok: true, message: "Agent endpoint ready for Phase 3." });
}
